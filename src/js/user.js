'use strict';
// function User(uName, uAge, uEmail, uPhoneNumber, uBankCardNumber, uGender) {
//     this.name = uName;
//     this.age = uAge;
//     this.email = uEmail;
//     this.phoneNumber = uPhoneNumber;
//     this.bankCardNumber = uBankCardNumber;
//     this.gender = uGender;
//
// }

class User{
    constructor(uName, uAge, uEmail, uPhoneNumber, uBankCardNumber, uGender){
        this.name = uName;
        this.age = uAge;
        this.email = uEmail;
        this.phoneNumber = uPhoneNumber;
        this.bankCardNumber = uBankCardNumber;
        this.gender = uGender;
    }
}
